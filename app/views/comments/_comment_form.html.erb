<%= form_for :comment, :url => new_comment_url do |f| %>
  <%= text_area 'comment', 'comment', :cols => 100, :rows => 5 %>
  <br>

  <%= submit_tag 'Add Comment' %>

  <%= hidden_field 'commentable', 'commentable', :value => type %>
  <%= hidden_field 'commentable', 'commentable_id', :value => model.id %>
<% end %>